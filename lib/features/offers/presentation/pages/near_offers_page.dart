import 'package:flutter/material.dart';

class NearOffersPage extends StatelessWidget {
  const NearOffersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Near Offers'),
    );
  }
}
